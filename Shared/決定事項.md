# 決定事項

## 2026-09-08

- **プロジェクトL（発送番号のRMS一括反映）Gate 2 実装GO**（マスター確認）。マスターがゴールを再確認し、相違点3つをフライデーが補正のうえ承認。
  - **ゴール（確定版）**: クリックポストで印字 → クリックポストが出す「追跡番号入りCSV」をマスターが LIENmanager に読み込ませる → アプリが取込済みの注文データ（変換直前に最新同期）と**宛先**（郵便番号＋氏名＋住所）で突き合わせ → RMS「発送完了報告データ」用の6列CSV（Shift-JIS）を自前生成 → マスターが RMS へアップロード → 照合できた注文のお荷物伝票番号が一括で埋まる。
  - **マスター当初理解との相違3点（補正済み）**:
    1. 照合相手は「先に作ったCSV」ではなく**アプリ内の注文データ**。突き合わせキーは注文番号ではなく宛先の正規化一致。変換前に必ず最新同期する。
    2. **完全全件ではない**。1対1で素直に照合できた分＋同一宛先グループの代表1件のみ自動出力。同一宛先の複数注文の残り・住所表記ズレ・複数送付先・180日超・前回出力済みは画面表示のみで、マスターが手打ち。
    3. RMSアップ用CSVはテンプレ配布がないため**アプリが6列を自前生成**。送付先ID・発送明細IDは空欄（＝送付先1へ新規登録）、配送会社1003固定、発送日は画面で1回入力し全行に適用。
  - **マスターが承認した前提**: DBに `Order.shippingReportedAt` 1列追加（二度流し防止の目印。前回失敗API用の項目とは別）／対象は「発送待ち300×未出力」／変換前に必ず最新同期／プレビューで手入力済み行のチェックを外す／この変換を使う日は同じ注文を RMS で手打ちしない（どちらか一方に寄せる）。
  - **次**: グラムが Gate 2 第1弾を実装（最新化＋自動マッチ＋代表1件＋各区分の一覧＋6列SJIS CSVダウンロード＋べき等化）。要確認の手動対応付けUIは第2弾。工数M（2〜3人日）＋DB1列S。詳細 `Gram/Gate1_発送番号のRMS反映_2026-09-07.md`。

- **プロジェクトL Gate 2 第1弾を実装（2026-09-08・コミット `fa6e98e`）**。発送エントリー画面に
  「発送完了報告CSVを作る」パネルを追加。クリックポスト追跡番号CSV → 宛先で注文と自動照合 →
  RMSアップロード用6列CSV（Shift-JIS）をダウンロード。DBは `Order.shippingReportedAt`（nullable）1列追加のみ、
  前回失敗API用コードには非依存。対象は「発送待ち300×未出力」＋変換時に180日超スキップ、同一宛先の複数注文は
  代表1件のみ出力・残りは画面表示。新規ユニットテスト33件パス。既存テストは236パス/3失敗（3失敗は前からある
  RMSブラウザ操作テストで無関係）。詳細 `Gram/Gate2_発送番号のRMS反映_2026-09-08.md`。
  **次はマスターの実地テスト**（少数の注文で 変換→DL→RMSアップロード→反映確認）。クリックポストCSVの実列名が
  想定と違えばグラムへヘッダー行だけ共有。

## 2026-09-07

- **主力商品ページ改善の画像作りは Canva 案を全て破棄し、方針を作り直す**（マスター指示）。
  マイクが 2026-08-29 に Canva で作った下書き16案（画像1〜4×各4案）と、制作指示書
  `CMO/商品画像1-4_制作指示書_2026-08-29.md` を削除。今後の画像の作り方（誰が・どのツールで・
  どこまでを下書きにするか）はマスターが方針を決めてから再開する。それまで画像1〜4の制作には着手しない。
  ページ改善プロジェクト（D）自体は継続。見出し・透け感・首丈の文案とレビュー根拠は有効。
- **週次販売数字**: 8/24〜8/30 と 8/31〜9/6 の2週分を記録済み（`CMO/販売状況.md`）。8/31〜9/6 はスーパーSALEの山を含む。
- **発送番号のRMS一括反映を検討開始**（マスター要望）。クリックポストのお問い合わせ番号を RMS の「お荷物伝票番号」欄へ
  手コピペしている作業をなくす。**方式は CSV 変換**（API／ブラウザ書き戻しは前回失敗のため不採用）:
  クリックポストの追跡番号入りCSV → LIENmanager で RMS一括アップロード用CSV（発送完了報告データ・6列固定・Shift-JIS）へ変換 → マスターがRMSへアップロード。
  2026-09-07 グラムが Gate 1 完了（改訂3、`Gram/Gate1_発送番号のRMS反映_2026-09-07.md`）。テンプレDLは無いので6列CSVを自前生成。
  送付先ID・発送明細ID は空欄（＝送付先1へ新規登録）。対象は「発送待ち×未出力」に厳密に絞り、変換前に最新同期＋プレビューで人が確認。
  配送会社＝1003（日本郵便）。同一宛先の複数注文は代表1件のみ出力。手入力済み注文は対象外。工数M（2〜3日）。
  **次セッションのマスター判断**: (1) DBに `Order.shippingReportedAt` 1列追加の可否 (2) 対象判定＋運用ルールの合意 → OKなら Gate 2 実装。
  起票 `Gram/課題_発送番号のRMS自動反映_2026-09-07.md`、進捗はプロジェクト L。

## 2026-08-29

- **GitHubリポジトリ `marutaka-lien/LIEN-AI` を非公開(Private)にした**。確認したところ
  public だったため、マスターがその場で「Change to private」を実行。事業構想・売上の実数字・
  自動化の仕組み・RMS/ClickPostの操作情報が含まれるため public 不可。マイク(Codex)が
  アクセスできるかは次の pull/push で確認し、不可なら Collaborator 追加で対応する。
- **アプリ常時起動の再起動テストに合格**。PC起動直後に起動済み・再起動しても正常を
  マスターが確認。以後はアプリが閉じていた場合のみマスターが報告する。
- **主力商品レビュー分析（直近50件）完了**。購入理由・不満点・使う場面の3分類でまとめ、
  差別化仮説「夏でも首元を隠したい大人の女性」を「支持する」と判定。首元を隠す・守る目的14件、
  夏の着心地への言及28件。元データ `BusinessData/reviews/`、結論 `CMO/主力商品レビュー分析.md`。
  商品ページ変更は今週は行わず提案として保留。
- **週次販売数字の様式を統一**（数字は変更なし）。(1)両CSVの売上列を `売上円` に統一
  (2)RMS画面表記と保存欄名の対応を手順書に追記 (3)月次CSVに `種別` 列を追加し通算行と月次行を区別。
  フライデーが反映済み。詳細は `CMO/週次販売数字の取得手順.md` の「様式確認結果」。
- **週次数字の取得を2画面方式にした**（フライデーがRMS実画面を確認）。RMS「店舗売上」画面には
  販売個数が無いと判明したため、売上・売上件数は「店舗売上」、販売個数は「商品別売上」の総計から取る。
  売上の定義（税込・送料除外・クーポン後）は不変。のし・ラッピング代は売上に含む点を明記。
  手順は `CMO/週次販売数字の取得手順.md`、統一ルールは `Shared/数字の定義.md`。マイクは次回のpullで取り込む。
- **主力商品ページ上部の改善を進めることを承認**（マイクの提案。中心メッセージ「夏も、首元を隠して涼やかに。」）。
  変える3点は上部見出し・透け感の伝え方・首丈約11cm。商品名・価格・広告は同時に変えない。
  まず変更前2週間の商品別の閲覧数・購入率を記録し、それから画像1〜4と説明上部を変更、変更後2週間を記録して
  「続ける・戻す・追加改善」を判断する。判定は購入率（5％以上改善、閲覧数・返品・問い合わせを悪化させない）。
  提案は `CMO/主力商品ページ改善提案_2026-08-29.md`。
- **商品別の閲覧数・購入率はRMSで取れる**（フライデーがRMS実画面を確認）。画面は
  「アクセス・流入分析 → 商品ページ」。商品管理番号で絞り、アクセス人数・転換率・売上件数・売上個数を読む。
  手順は `CMO/商品別アクセス・購入率の取得手順.md`。店舗カルテの「転換率・客単価」は店舗全体の数字なので商品評価には使わない。
- ~~**主力商品ページ改善の画像は Canva で作る**~~ → **2026-09-07 マスター指示で取り消し**。
  マイクが Canva で作った下書き16案（各画像4案）と制作指示書 `CMO/商品画像1-4_制作指示書_2026-08-29.md` は
  すべて破棄。画像の作り方の方針をマスターが決め直してから再開する。詳細は 2026-09-07 の決定事項を参照。
- **LIENmanager のCSVダウンロードで一部文字化け**（マスター発見。「-」が「?」になる／画面表示は正常）。
  グラムの Gate 1 分析で原因特定: ClickPost「まとめ申込」用CSVを Shift_JIS で書き出す際、住所・氏名の
  特殊ダッシュ（EN/EM DASH・マイナス記号など）が CP932 に無く「?」に置換されていた。他のCSV機能は無関係。
  → 2026-08-29 マスター決定: **A案（ダッシュ類を半角ハイフン「-」へ正規化）＋ C案（安全網）**で実装。
  C案は注文をCSVから外さず「直せなかった文字がある注文」を画面で目立つ一覧として知らせる方式
  （外すと発送ラベルが作られず荷物が出ない事故になるため）。ClickPost CSV は仕様上 Shift_JIS 必須で
  文字コードは変えない。グラムが実装→テスト→動作確認まで進め、コミットはマスター承認後。
  詳細は `Gram/課題_CSV文字化け_2026-08-29.md`。

- **Codex UI/UX レポート §10 の6件をマスターが回答**（`LIENmanager/docs/CODEX_UI_UX_REPORT.md`。
  画面の第1段階は実装済みで承認不要。Phase 2 の設計前提としての回答）:
  1. **主要利用者 = 経営者と発送担当の両方（兼任）**。片方向けに寄せず、発送作業と経営判断の両方が1画面で回る構成にする。
  2. **発送締切 = その日の13時を目安（厳密でない）**。実運用は「注文が入ったのを確認したらできるだけ早く処理」。
     → 画面は 13:00 で赤く「遅延」と鳴らす作りにしない。「未処理の件数」と「注文が入ってからの経過」を見せ、
     気づける程度の弱い注意表示にとどめる。締切は固定ルールでなく目安として扱う。
  3. **売上データの取得 = 楽天API と マスターが RMS を見て取得、の両方**。当面は手入力の経路を用意し、
     将来 API 連携を足せる形にする（どちらか一方に決め打ちしない）。
  4. **SQLite のバックアップ・保存期間 = Codex の推奨案で進める**（マスターは方式を指定しない）。
  5. **マーケ分析をアプリに入れる範囲 = 「人間が数字を見て判断する」画面ではない**。販売判断はほぼマイクが行う。
     マスターがこの画面に求めるのは、**人間がやるべき 0→1（新しいアイデア出し）が進みやすくなる「素材」が目に入ること**。
     → インサイト画面は、整った KPI レポートではなく、アイデアの種になる材料
     （購入者の声・レビューの断片・想定外の変化・市場の兆し等）が自然と目に入る作りにする。狙いは着想の誘発。
  6. **画面モックの採否 = フライデー/Codex に一任**。そのまま採用してもよいし、グラムに評価させてもよい。
     → グラムに軽い評価パスを1回入れてから採用する（手戻り防止）。

- **CSV文字化け修正をコミット＆プッシュ**（2026-08-29 マスター「1 yes」）。フライデーがコミット `96022de`
  "fix: normalize CP932-unsafe dashes in ClickPost CSV, warn on residue"（10ファイル / +402 -18）。
  コミット前に clickpost テスト46件パス、本修正ぶんの tsc クリーンを再確認。origin/main へプッシュ済み。
  `Gram/課題_CSV文字化け_2026-08-29.md` はクローズ。
- **既存テスト不具合2件は当面保留**（2026-08-29）。マスター方針「簡単なタスクだけすぐ処理」。原因分析が必要で
  すぐ終わらないため、着手は別途GOを待つ。`Gram/課題_既存テスト不具合_2026-08-29.md` は起票のまま。

- **仕事1（週次販売数字）完了**（2026-08-29）。8/17〜8/23：売上133,333円／注文23件／販売個数32個／
  1注文あたり 約5,797円・1.39個。`CMO/販売状況.md`・`BusinessData/sales/週次販売実績.csv`＋商品別内訳CSV。コミット `1bc27ff`。
- **仕事2（主力インナーの残る金額）→ グランドマスター待ち**（2026-08-29 マスター指示）。費用6項目は社内実費のため
  `GrandMaster/確認事項.md` Q1 に起票。回答後に xlsx 入力→`販売状況.md` 記録。
- **仕事3（ページ改善の変更前記録）完了**（2026-08-29）。主力商品 1000000115 の商品ページ分析2週間ぶん。
  8/10〜16：閲覧181/購入6/転換率3.31%/個数10。8/17〜23：閲覧190/購入4/転換率2.11%/個数5。
  値引き・広告・欠品は3項目とも「不明」（マスター回答）。元CSV `BusinessData/experiments/raw/`、集計 `主力商品ページ改善_記録.csv`。
  申し送り：変更前の転換率が週で大きく揺れる（母数約185）ため、判定「購入率5%以上改善」は2週平均比較＋留保でマイクが調整。
- **仕事4（マイクの改善案への回答）**（2026-08-29 マスター回答）:
  1. **前後の見分け方**：布タグの位置で分かる。**タグが左側に来る向きが「前」**。
  2. **タグ仕様**：**布タグの縫い付け**（生地への直接プリントではない）。※レビューの「チクチク・タグ」不満の一因の可能性。
  3. **洗濯表示**：商品ページの実表記を参照、との指示。フライデーが商品ページ本文を確認 → 正式な洗濯表示（絵表示の文章）は
     本文テキストには無く、スペック欄キーワードに「洗濯可 手洗い可」、説明に「繰り返し洗っても風合いが長持ち」とあるのみ。
     画像内にある可能性。→ マイクは洗濯Q&Aを断定で書かず、洗濯ネームの実文言をマスター確認後に記載（`提案一覧`/`改善提案`のQ&A該当箇所）。
  参考（フライデーが商品ページから取得。マイクの画像・原稿用）：実寸 M/L＝肩幅34.5/35.5・身幅42/46・着丈60.5/61・
  アームホール17.5/19・裾幅42/47、首高さ11/11cm（両肩頂点を結ぶ直線の中心から垂直計測）。綿100%（超長綿）、透け感あり、
  伸縮性あり、重さ M約63g/L約70g、日本製、メール便配送。価格2,980〜3,980円（色により一部値下げ中）、レビュー4.50（96件）。

## 2026-08-28

- CodexのCMOエージェントを正式に組み込み、名前を「マイク」とする。
- フライデーは事業全体、マイクは販売、グラムはLIENmanager実装を担当する。
- マスターが業務上の最終判断を行う。
- 調査 → 提案 → 承認 → 実行 → 効果確認を共通の運営サイクルとする。
- 全員に影響する情報は、個別AIの記憶だけでなく `C:\lien_AI_manage\Shared\` に記録する。
- 本番変更、費用発生、外部公開、重要な方針はマスター承認後に行う。

## 2026-08-28(追加分)

- **作業場所を分ける**。フライデーは `C:\lien_AI_manage\` を直接編集する。マイクは
  Codex側の別のコピーで作業し、完成した変更を GitHub へ送る。共通の最新版は GitHub の `main`。
- **ファイルの担当**。`CEO\`=フライデー / `CMO\`=マイク / `Gram\`・`LIENmanager\`=グラム /
  `README.md`=フライデー / `Shared\今週の優先事項.md`・`承認待ち.md`・`決定事項.md`=フライデー /
  `Shared\数字の定義.md`=マイク。マイクが `Shared\` へ追加したい内容は原則フライデーへ伝え、
  フライデーが反映する。マスターからマイクへ直接指示があった場合のみマイクが直接変更してよい
  (その場合も作業前に GitHub 最新版を取り込み、作業後に GitHub へ反映する)。
- **フォルダ名は `C:\lien_AI_manage\` のまま**にする(改名しない)。ドキュメント内の
  古い `C:\lien\` 表記は実体に合わせて修正する(2026-08-28 フライデー担当ぶんは修正済み。
  `CMO\` 配下はマイク、`Gram\`・`LIENmanager\CLAUDE.md` はグラムが修正する)。
- **GitHub への送信権限**。フライデーとグラムが commit から送信まで自分で行えるよう、
  マスターが `C:\lien_AI_manage\.claude\settings.json` に許可設定を作る。作成前は
  フライデーが変更をまとめ、マスターが送信を1行実行する。
- **フライデーからマスターへの報告は平易な言葉で**。順番は 結論 → 完了したこと →
  マスターが決めること → 注意点。専門用語は日常語に置き換える。詳細は `CEO\CLAUDE.md`。

## 2026-08-28（今週の仕事の承認）

- 今週進める仕事を3つに確定した（詳細と完了条件は `Shared\今週の優先事項.md`）。明日以降に着手する。
  1. 毎週見る販売数字を「決まった形」で取れるようにする（マイク主担当）
  2. 主力商品の「実際に残る金額」を1商品分そろえる（マイク＋マスターの実費提供）
  3. 主力商品レビューから購入理由・不満・使用場面を1枚にまとめる（マイク）
- 楽天RMSへのログインは 2026-08-28 に完了。数字の取り出しはマスターが対応する。
- 3件とも調査・準備であり、商品ページや広告の変更は含まない。
