@AGENTS.md

## CEOプロジェクトとの連携(2026-08-25設定)

このプロジェクトはLIENの経営・優先順位判断を担う `C:\lien\CEO` から、実装タスクとして
委任される形で作業が発生することがある(Agentツールによるサブエージェント委任)。
経営判断(何を・なぜ)はCEO側の役割であり、このプロジェクト側では技術的な実現方法・
実装・テストに専念する。既存の運用ルール(Gate方式の段階承認、個人情報の許可リスト方式、
AGENTS.mdより実コード優先)は委任元がCEOであっても変わらず適用する。
