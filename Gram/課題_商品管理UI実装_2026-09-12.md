# 課題: 商品管理画面のUIを先行実装する（プロジェクトM・見た目のみ）

**【クローズ済み・2026-09-12】** マスター指示により、この起票内容はグラムへ依頼せず**フライデーが直接実装**した（コミット `e263c2b`）。あわせてアプリ全体のテーマ刷新も実施。詳細は `Master/プロジェクト一覧.md` プロジェクトM参照。以下は起票時の指示内容（参考として保存）。

---

起票日: 2026-09-12（フライデー）
担当: ~~グラム（LIENmanager）~~ → フライデーが実施
優先度: 中（Fの微調整より上、Lはクローズ済み）

## 背景・方針（2026-09-12 マスター指示）

プロジェクトM（商品登録機能）は、グランドマスターQ3（何を自動化したいか）の回答がまだ無い。
マスターの方針: **機能（API連携・実データ保存）は後回しにして、先にUIだけを作り、
できることが安定してきた段階で機能を追加していく。**
そのため今回のタスクは「画面と導線だけ作る」こと。API連携・DB保存・RMSへの実書き込みは含めない。

## 参照するデザイン資料

`LIENmanager/docs/design/product-management-m-2026-09-10/` 配下（今回コミットしたもの）。

- **`product-list-export/` が最新・完全版**（2026-09-11 12:03 出力）。こちらを正とする。
  - `商品一覧 v2-要件版.dc.html` … 商品一覧画面の要件反映版（**これを最優先で参照**）
  - `商品一覧.dc.html` / `商品一覧 v1.dc.html` … 検討過程の版（参考程度）
  - `商品登録.dc.html` … 新規登録画面
  - `発送エントリー.dc.html` / `発送エントリー v2.dc.html` … 既存のF実装と重複するので**参照不要**（Fはすでに実装・コミット済み）
  - `今日のオペレーション.dc.html` … 既存ダッシュボードと同系統のトーン確認用（**実装対象ではない**）
- `dashboard-export/`（2026-09-11 11:10 出力）は上記より古い版。基本は無視してよいが、`product-list-export` に無い差分があれば見比べる程度に。

これらは `.dc.html` 形式（デザインツールのプレースホルダー入りHTML。`{{ query }}` や `<sc-for>` はダミーの繰り返し表示指定で、実データではない）。実装時は既存の `LIENmanager` のコンポーネント・トークン規約（`globals.css` の `.dark`、既存の shadcn/ui ベース）に翻訳して作り直すこと。HTMLをそのまま埋め込まない。

## やること

1. **ナビゲーション追加**: サイドバーに「商品管理」を追加（デザイン案のサイドバー構成: 今日の運営／発送業務／レビュー／商品管理／設定）。既存のサイドバー構成・命名と齟齬がないか確認し、必要なら命名は既存踏襲を優先。
2. **商品一覧画面**: `商品一覧 v2-要件版.dc.html` を基に、検索・絞り込みチップ・並び替え（売れている順／在庫が少ない順／購入率が高い順／更新が新しい順）・商品カード一覧のレイアウトを実装。
   - データは**モック／ダミーでよい**。実際の楽天商品APIには繋がない。
   - タブ構成: 「商品一覧」「新商品登録」「予約状況」の3タブ（デザイン案どおり）。「予約状況」タブは空でもよい（Q3回答後に中身を決める）。
3. **新商品登録画面**: `商品登録.dc.html` を基に、入力フォームのレイアウトのみ実装。**保存ボタンは押しても実際には何も送信しない**（トースト等で「下書き保存しました（デモ）」程度の見た目だけでよい）。
4. コードは既存の `LIENmanager/src` の構成・命名規約に合わせる。DBスキーマ変更・API新設は不要（このGateでは行わない）。
5. build・型チェック・既存テストが緑であることを確認。

## やらないこと（今回のスコープ外）

- 楽天商品API（`itemapi_2.0.*`）との実連携
- DBへの下書き保存・スケジュール実行・承認フロー
- 画像アップロード（R-Cabinet API）の実装
- 「予約」機能の具体的な中身（Q3の回答待ち）

## 完了条件

- サイドバーから「商品管理」→商品一覧・新商品登録の画面へ遷移できる。
- 見た目がデザイン案（特に `商品一覧 v2-要件版.dc.html`）に沿っている。
- ダミーデータ・ダミー動作であることが分かる形で実装（捏造データに見えないよう、明らかにモックと分かる程度の配慮でよい）。
- build/tsc/test 悪化なし。

## 注意

- 個人情報・実在の商品データはチャット・コミットに出さない（ダミー商品名でよい）。
- `.env` は編集しない。
- 完了したらマスターに実画面を見てもらい、OKが出たらコミット。
