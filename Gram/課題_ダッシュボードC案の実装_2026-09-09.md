# 課題: ダッシュボードC案を実アプリに反映（プロジェクトF Phase 2）

起票日: 2026-09-09（フライデー）
担当: **フライデー**（2026-09-09 マスター指示で グラム→フライデー に再変更。「私の確認なしで進めて、モックのデザインをそのままアプリに反映するのがゴール」）
段階: **第1弾コミット済み（2026-09-09・コミット `0a7ebaf`）**。マスターが翌日 実画面で確認 → OKなら完了。

## 第1弾でやったこと（2026-09-09）

- `src/app/dashboard/page.tsx` をC案（左「作業」／右「把握」の2ゾーン）に作り直し。サーバーコンポーネント、全ブロック実データ（取れないものは空/0状態、捏造なし）。
- 左: 要確認事項（実データのアラート＝注文確認待ちの有無。他ソースは別タスク）／主要指標2×2（発送待ち・CSV未出力・本日発送済み・レビュー未返信）／出荷パイプライン4段（滞留を1つ強調）／次に実行する推奨アクション（CSV作成は `/automation` へ誘導）。
- 右: 本日の処理推移（自前SVG折れ線。受注累計＋発送完了累計、13:00 締切目安線。既存 `orderedAt`/`shippingReportedAt` から算出、DB追加なし）／最新アクティビティ（直近ジョブ5件）／発送待ち0件なら「本日の発送は完了」。
- 新規: `src/lib/operations-dashboard.ts`（純粋関数＋ユニットテスト10）、`order.repository` に `countOperationsOverview()` / `getTodayOrderTimeline()`、`review.repository` に `countUnreplied()`。
- ルートに `loading.tsx` / `error.tsx`。トークンは `globals.css` の `.dark`。Nocturne DS 不使用。チャートライブラリ追加なし。
- 旧ダッシュボードの未使用ウィジェット（automation-status-hero / kpi-card / latest-job-card / module-status-panel / dashboard-status）を削除。
- `npm run build` 成功 / `tsc` は本変更ぶんクリーン（既存の review-reply-reconciliation.test の型エラー1件だけ残存＝旧プロジェクトH、取り下げ済み）/ `vitest` は既存の rms-browser-client 3件失敗のみ（新規10件パス）。
- 動作確認: `/dashboard` HTTP 200、エラーオーバーレイなし、実データ（注文確認待ち・当日推移）が表示されることを確認。

## 残り（第2弾以降・マスター確認後）

- モックにある「本日の処理推移」のエラー三角マーカー（CSV文字化け等の注記）は実ソース未接続のため未実装。→ `Gram/課題_RMS同期失敗の可視化_2026-09-09.md` と一緒に扱う。
- 要確認事項のアラート種別を増やす（住所不備・同一宛先の複数注文・支払い未確認 等）。
- 「対象者CSVを作成」をダッシュボードから直接実行（現状は `/automation` へ誘導）。やるなら client 化＋確認ダイアログ。
- モバイル（390px）最適化＝A案の縦積み（別タスク）。今回は「崩れない」までは未検証（要 実機/幅チェック）。
- 視覚の細部（余白・文字サイズ・SVGの目盛り）をモック（`preview.html`）と並べて微調整。

---

## 参考（第1弾の元指示。以下は当初の想定）

## 経緯

- 2026-09-08: マスター決定でダッシュボードの方向は **C案（ハイブリッド型）**。Claude Design で視覚モック作成済み。
- 当初は Codex（画面担当）が Gate 1 計画から実装する予定だった。
- **2026-09-09 マスター指示: 「Codex の評価はやめた。そのままアプリに反映して」** → 担当を Codex からグラムに変更。計画レビューの往復も省く。

## やること

`src/app/dashboard/page.tsx`（現「今日のオペレーション」）を、C案（ハイブリッド型）の視覚モックどおりに作り直す。

### 仕様（そのまま使う）

- **実装依頼の本体**: `LIENmanager/docs/CODEX_DASHBOARD_C_IMPLEMENTATION_PROMPT.md`
  ― レイアウト、左右ゾーンの中身、状態設計（loading/empty/error/disabled/success）、守る/やらない、完了条件まで詳細に書いてある。**宛先が「Codex」になっているが中身はそのまま有効**。読み替えて使う。
- **表示確認用**: `LIENmanager/docs/design/dashboard-c-2026-09-08/preview.html`（ブラウザでそのまま開ける。右上で状態切替）
- **視覚仕様の正**: 同フォルダ `今日のオペレーション.dc.html` ＋ `README.md`
- **設計ブリーフ**: `LIENmanager/docs/DASHBOARD_C_DESIGN_BRIEF_2026-09-08.md`
- **UX方針**: `LIENmanager/docs/CODEX_UI_UX_REPORT.md`（§4/§5/§8/§9）

### 今回のマスター決定（依頼書からの変更点2つ）

1. **担当＝グラム**。Gate 1 の計画提出＋マスター確認の往復は省く。実装を進めてよい。
   （依頼書「進め方（Gate方式）」の 1. は飛ばす。2. の自己評価と 3. のコミット承認は残す。）
2. **「本日の処理推移」グラフ＝既存データの範囲で作る。DB変更なし**。
   `Order.orderedAt` / `shippingReportedAt` / `csvExportedAt` など今あるフィールドから時間帯別の累計を算出できる範囲で描く。
   算出できない時間帯・系列は空欄（値を捏造しない。必要なら「一部の時間帯はデータなし」の注記）。
   `OperationalSnapshot` 的な集計テーブルの新設は**しない**（将来必要になったら別課題で起票）。

### データの扱い（依頼書のとおり）

- 実データが取れるものは繋ぐ（発送待ち・CSV未出力・本日発送済み・レビュー未返信・パイプライン各段・アクティビティ）。
- 取れないものは loading / empty のまま出す。**モックの数字をそのまま焼き込まない**。
- 画面内の「MOCK（架空データ）」表示は、実データ接続前の暫定。接続済みの箇所からは外す。

### トークン・スタック

- 色・間隔・角丸・影・フォントは `src/app/globals.css` の `.dark` トークンから。生 hex を新規に置かない。
- Claude Design 随伴の `_ds/nocturne-*`（blurple）は**使わない**。
- Tailwind v4 クラス / トークンに置き換え、インラインスタイルはやめる。アイコンは `lucide-react`。
- アプリはダーク固定。ライトは不要。
- チャートライブラリ・外部フォントの新規追加なし。推移グラフは自前 SVG（`components/dashboard/` にSVGコンポーネント）。

## 完了条件（依頼書より）

- `npm run build` 成功、`npx tsc --noEmit` は本変更ぶんクリーン、`eslint` エラーなし。
- `npm run test` は既存比で悪化なし（`main` に元からある失敗3件＝RMSブラウザ操作テストは対象外。※旧「既存テスト不具合2件」課題は 2026-09-09 マスター指示で取り下げ済み）。
- デスクトップ1440pxで `.dc.html` と情報構造が一致。`md` 以下で縦積みに破綻なく落ちる（モバイル最適化は別タスク、今回は「崩れない」まで）。
- loading / empty / error / disabled / success が各ブロックで確認できる。

## 進め方

1. 実装（グラム）。
2. グラム自身で完了条件を確認（build / tsc / eslint / test / 1440px 目視 / 各状態）。
3. **マスターへ結果報告**（フライデー経由）: 触ったファイル、実データに繋いだ箇所／loading・emptyのまま残した箇所、推移グラフで出せた範囲、build・testの結果、既知の制約。
4. マスター承認後にコミット／プッシュ。
5. 反映後、マスターが実画面で確認。

## 注意

- 個人情報はチャット・ログ・コミットに出さない。
- 前回失敗した RMS書き戻しAPIのコードには触れない。
- `AGENTS.md`（LIENmanager）: この Next.js は通常と違う。コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読む。
