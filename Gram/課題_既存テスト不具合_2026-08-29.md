# 課題: 既存テストの不具合2件 ＜クローズ・取り下げ 2026-09-09＞

報告日: 2026-08-29（グラムがCSV文字化け修正の実装中に発見）
担当: グラム（LIENmanager）
段階: **取り下げ（2026-09-09 マスター指示「もう必要ない」）**。着手しない。

> 対象の失敗テストはアプリの稼働に影響しない（テスト側だけの失敗）。RMS発送ブラウザ操作は
> プロジェクトLで書き戻し方式から外した経路。詳細は `Shared/決定事項.md` 2026-09-09。
> CI を緑にしたい場合は当該4件を skip 指定にする小作業を別途起票する。

以下は当時の記録（履歴として残置）。

## 背景

CSV文字化け修正（`課題_CSV文字化け_2026-08-29.md`）の作業中、`main` の時点で既に存在する
テストの不具合が2件見つかった。今回の変更とは無関係のため、その作業では触っていない。

## 不具合

1. **RMS発送ブラウザ操作のテストが3件失敗**: `src/server/integrations/rms/__tests__/rms-browser-client.test.ts`。
2. **レビュー返信照合のテストで型エラー1件**: `src/server/review/__tests__/review-reply-reconciliation.test.ts`。

## 判断してほしいこと

- 今すぐ直すか、後回しにするか。
- 直す場合、グラムに Gate 1（原因分析・影響範囲・直し方の提案）から依頼する。

## 注意

- 個人情報はチャット・報告に出さない。件数・カラム名・ファイルパスのみ。
- git 操作はしない。分析結果はフライデー経由でマスターへ。
